<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
	<div id="error_explanation">
	  <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>
 
	  <ul>
	  <% @task.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
	  <% end %>
	  </ul>
	</div>
  <% end %>
 
  <div class="field">
	<%= f.label :Evento %><br />
	<%= f.text_area :description %>
  </div>
  <div class="field">
	<%= f.label :Status %><br />
	<%= f.select :status, (["Mudança", "Incidente", "Info"]) %>
  </div>
  <div class="field">
	<%= f.label :Critidade %><br />
	<%= f.select :criticidade_id, @criticidades.collect { |a| [ a.name, a.id ] }  %>
  </div>
  <div class="field">
	<%= f.label :Executor %><br />
	<%= f.select :author_id, @authors.collect { |a| [ a.name, a.id ] }  %>
  </div>
  <div class="field">
	<%= f.label :Cliente %><br />
	<%= f.select :category_id, @categories.collect { |c| [ c.name, c.id ] } %>
  </div>
  <div class="field">
	<%= f.label :Site %><br />
	<%= f.select :site_id, @sites.collect { |c| [ c.nome, c.id ] } %>
  </div>
  <div class="actions">
	<%= f.submit %>
  </div>
<% end %>
