<div id="stylized" class="myform">
<%=  form_tag({:controller => @controller, :action => "enviar_email", :id => @id} , remote: true,  authenticity_token: false  ) do |f|  %>
<!--%=  form_for :enviar_email,  :remote => true, :html => {:id => "email"}, :remote => true do |f|  %-->

<label>Selecione o Template
</label>

<%= hidden_field_tag 'id', @id %>
<%= select_tag  :template_id, options_from_collection_for_select(@templates_email, "id", "full_nome")  %>
<div class="buttons">
    <%= submit_tag "Enviar Email" %>
</div>
</br>

<% end %>
</div>

<!-- <:url => url_for(:controller => 'email', :action => 'enviar_email'), > -->
