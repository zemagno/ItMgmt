<%= form_for(@status_checklist) do |f| %>
  <% if @status_checklist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_checklist.errors.count, "error") %> prohibited this status_checklist from being saved:</h2>

      <ul>
      <% @status_checklist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :icon %><br />
    <%= f.text_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
