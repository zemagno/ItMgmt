<%= form_for(@parametro) do |f| %>
  <% if @parametro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parametro.errors.count, "error") %> prohibited this parametro from being saved:</h2>

      <ul>
      <% @parametro.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<label>Tipo
</label>
<%= f.text_field :tipo, :size => 50   %>
<div class="spacer"></div>

<label>Subtipo
</label>
<%= f.text_field :subtipo, :size => 50   %>
<div class="spacer"></div>

<label>Parametros
</label>
<%= f.text_area :valor, :cols => 200, :rows => 40   %>
<div class="spacer"></div>


<div class="actions">
 
    <button type="submit" id="SalvarParametro">Salvar Parametro</button>
</div>
<% end %>
<div class="spacer"></div>
