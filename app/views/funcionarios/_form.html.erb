<%= form_for(@funcionario) do |f| %>
  <% if @funcionario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@funcionario.errors.count, "error") %> prohibited this funcionario from being saved:</h2>

      <ul>
      <% @funcionario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>




<label>Cipa ?
<span class="small">Integrante da Cipa? </span>
</label>
<div class="field">
    <%= f.check_box :cipa %>
</div>

<div class="spacer"></div>


<label>Brigadista ?
<span class="small">É brigadista? </span>
</label>
<div class="field">
    <%= f.check_box :brigadista %>
</div>

<div class="spacer"></div>

<label>Sem Estacao ?
<span class="small">Esse usuario nao usa estação BRQ? </span>
</label>
<div class="field">
    <%= f.check_box :semEstacao %>
</div>

<div class="spacer"></div>


<label>
<span class="small">Observacao</span>
</label>
<%= f.text_field :observacao, :size => 70 %>
<div class="spacer"></div>

<label>É externo e possui ramal ?

</label>
<div class="field">
    <%= f.check_box :ramalSendoExterno %>
</div>

<div class="spacer"></div>

<div class="actions">
    <%= f.submit %>
</div>
<div class="spacer"></div>
<% end %>
