<%= form_for(@status_pedido) do |f| %>
  <% if @status_pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_pedido.errors.count, "error") %> prohibited this status_pedido from being saved:</h2>

      <ul>
      <% @status_pedido.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
