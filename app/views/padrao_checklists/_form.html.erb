<%= form_for(@padrao_checklist) do |f| %>
  <% if @padrao_checklist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@padrao_checklist.errors.count, "error") %> prohibited this padrao_checklist from being saved:</h2>

      <ul>
      <% @padrao_checklist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :itens %><br />
    <%= f.text_area :itens %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
