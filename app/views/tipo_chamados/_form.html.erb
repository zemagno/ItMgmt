<%= form_for(@tipo_chamado) do |f| %>
  <% if @tipo_chamado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_chamado.errors.count, "error") %> prohibited this tipo_chamado from being saved:</h2>

      <ul>
      <% @tipo_chamado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
