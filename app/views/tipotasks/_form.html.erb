<%= form_for(@tipotask) do |f| %>
  <% if @tipotask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipotask.errors.count, "error") %> prohibited this tipotask from being saved:</h2>

      <ul>
      <% @tipotask.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
