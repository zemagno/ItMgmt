<%= form_for(@status_incidente) do |f| %>
  <% if @status_incidente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_incidente.errors.count, "error") %> prohibited this status_incidente from being saved:</h2>

      <ul>
      <% @status_incidente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :ordem %><br />
    <%= f.number_field :ordem %>
  </div>
  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
