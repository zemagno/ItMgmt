<h1>Listing status_chamados</h1>

<table>
  <tr>
    <th>Descricao</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @status_chamados.each do |status_chamado| %>
  <tr>
    <td><%= status_chamado.descricao %></td>
    <td><%= link_to 'Show', status_chamado %></td>
    <td><%= link_to 'Edit', edit_status_chamado_path(status_chamado) %></td>
    <td><%= link_to 'Destroy', status_chamado, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Status chamado', new_status_chamado_path %>
