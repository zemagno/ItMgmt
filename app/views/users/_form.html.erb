<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
<label>Perfil
</label>
<%= f.text_field :roles, :size=>100 %>

<div class="spacer"></div>

<label>Login
</label>
<%= f.text_field :login, :size=>30 %>

<div class="spacer"></div>
  <div class="actions">
   <button type="submit" id="SalvarUser">Salvar Usuario</button>

  </div>
<% end %>
<div class="spacer"></div>
