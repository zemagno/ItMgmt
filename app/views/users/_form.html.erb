<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.label "Perfil" %>
    <%= f.text_field :roles, :size=>100 %>
  </div>

  <div class="field">
    <%= f.label "Login" %>
    <%= f.text_field :login, :size=>30 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
