<!-- onchange="location.reload() -->
<h1>CMDB</h1>
<!-- 
<% [:info, :error].each do |key| %>
   <% if flash[key] %>
   <div class="flash <%= key %>" id="flash">
     <%= flash[key] %>
   </div>
   <% end %>
 <% end %>

 -->
<%= link_to '[Novo CI]', new_ci_path, :id => "zNovo" %>


<%= will_paginate @cis %>

<%= form_tag(cis_path, :method => "get") do %>
 
  <%= text_field_tag(:search, @search, :size => 50) %>
  <%= submit_tag("Filtrar") %>
  <!--%= submit_tag("Estou com sorte") % -->
  <%= select_tag :view_default_ci, options_for_select(@views_ci, @view_default_ci), :onchange => "this.form.submit();"  %>

<% end %>

<table>
  <tr>
    <th></th>
    <th>Chave CI</th>
<% @fields[0].each do |f| %>
   <th><%= f %> </th>
<% end %>
  </tr> 

<% @cis.each do |ci| %>
  <tr>
    <td><img src="/icons/<%=ci.status_icon%>" alt=" " height="16" width="16"></td>
    <td><%= link_to ci.chave, ci %></td>
    <% @fields[1].each do |f| %>
       <td><%= ci.send(f) %> </td>
    <% end %>
  </tr>
<% end %>
</table> 

<!--

<table>
  <tr>
    <th></th>
    <th>Chave CI</th>
    <th>Descricao</th>
    <th>Tipo</th>
    <th>Localidade</th>
    <th>Gestor</th>
    <th>Usuario(s)</th>
    <th>Ult ChgMgmt</th>
  </tr>

<% @cis.each do |ci| %>
  <tr>
    <td><img src="/icons/<%=ci.status_icon%>" alt=" " height="16" width="16"></td>
    <td><%= link_to ci.chave, ci %></td>
    <td><%= ci.descricao %></td>
    <td><%= ci.nice_tipoci %></td>
    <td><%= ci.site.nome %></td>
    <td><%= ci.Owner %></td>
    <td><%= ci.notificacao %></td>
    <td><%= ci.dataChange %></td>
    
  </tr>
<% end %>
</table>

-->

<%= will_paginate @cis %>


<br />