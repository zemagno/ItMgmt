<%=  form_for :exclusao, :remote => true, :html => {:id => "elimina_dependente"} do |f|  %>
<label>Elimina Dependente
<span class="small">Entre com a Chave </span>
</label>
<%= hidden_field_tag 'idci', @idci%>
<%= f.text_field :dependente %>
<div class="buttons">
    <%= f.submit "Eliminar" %>
</div>
</br>

<% end %>
